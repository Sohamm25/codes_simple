def mode():
    numbers = []
    while True:
        num = float(input("enter n"))
        numbers.append.num()
        if num == 0:
            break
        else:
            print("invalid")
    abc = sum(numbers)
    return abc
abcd = mode()
print("adadsis{}".format(abcd))
